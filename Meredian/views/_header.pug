header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Усі тури
  .header__logo
    img(src='/img/logo-white.png' alt='Логотип')
    <h2>Меридіан</h2>
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Вийти
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Фото користувача ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Увійти
      a.nav__el.nav__el--cta Зареєструватися